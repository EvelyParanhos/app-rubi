# Multi-stage or lightweight Nginx image for Frontend Prototype
FROM nginx:alpine
COPY src/main/resources/static /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
